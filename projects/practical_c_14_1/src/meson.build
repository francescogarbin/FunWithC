# This project compiles with the meson build system and the ninja backend.
# To compile and run check the README.md file in the project.

project('countchars', 'c', version:'1.0')
executable('countchars', 'main.c')

